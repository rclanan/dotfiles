# === TMUX CONFIGURATION FOR RUSTDESK REMOTE DEVELOPMENT ===

# Set default terminal to support 256 colors
set -g default-terminal "screen-256color"

# Enable mouse support for easier remote navigation
set -g mouse on

# Increase scrollback buffer size
set -g history-limit 10000

# Set escape time to 0 for better responsiveness over remote connections
set -sg escape-time 0

# Renumber windows automatically when one is closed
set -g renumber-windows on

# Start windows and panes at 1, not 0
set -g base-index 1
set -g pane-base-index 1

# === KEY BINDINGS ===

# Change prefix key to Ctrl-a (easier for remote access)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# Reload config with r
bind r source-file ~/.tmux.conf \; display "Config reloaded!"

# Split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %

# Switch panes using Alt-arrow without prefix (good for remote use)
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-Up select-pane -U
bind -n M-Down select-pane -D

# === VISUAL SETTINGS FOR REMOTE CLARITY ===

# Status bar styling
set -g status-bg colour234
set -g status-fg colour137
set -g status-left-length 20
set -g status-right-length 50
set -g status-left '#[fg=colour233,bg=colour241,bold] #S #[fg=colour241,bg=colour235,nobold]'
set -g status-right '#[fg=colour233,bg=colour241,bold] %d/%m #[fg=colour233,bg=colour245,bold] %H:%M:%S '

# Window status styling
setw -g window-status-current-style 'fg=colour1 bg=colour19 bold'
setw -g window-status-current-format ' #I#[fg=colour249]:#[fg=colour255]#W#[fg=colour249]#F '
setw -g window-status-style 'fg=colour9 bg=colour18'
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '

# Pane border colors
set -g pane-border-style 'fg=colour238'
set -g pane-active-border-style 'fg=colour51'

# === SAAS PLATFORM DEVELOPMENT SESSION ===

# Create a development session on startup
new-session -d -s 'saas-dev' -c '/home/rclanan/dev/projects/work/saas-platform'

# Window 0: Main development
rename-window 'main'
send-keys 'cd /home/rclanan/dev/projects/work/saas-platform' C-m
send-keys 'ls -la' C-m

# Window 1: Backend API
new-window -t 'saas-dev' -n 'backend' -c '/home/rclanan/dev/projects/work/saas-platform/apps/backend-api'
send-keys 'echo "Backend API - Ready for development"' C-m

# Window 2: Frontend Platform Admin
new-window -t 'saas-dev' -n 'admin-frontend' -c '/home/rclanan/dev/projects/work/saas-platform/apps/frontend-platform-admin'
send-keys 'echo "Platform Admin Frontend - Port 3001"' C-m

# Window 3: Additional Frontend (Port 3000)
new-window -t 'saas-dev' -n 'tenant-frontend' -c '/home/rclanan/dev/projects/work/saas-platform'
send-keys 'echo "Tenant Frontend - Port 3000"' C-m

# Window 4: Docker services
new-window -t 'saas-dev' -n 'docker' -c '/home/rclanan/dev/projects/work/saas-platform'
send-keys 'echo "Docker Services - Use ./start-platform-buildkit.sh"' C-m

# Window 5: Docker management
new-window -t 'saas-dev' -n 'docker-ui' -c '/home/rclanan/dev/projects/work/saas-platform'
send-keys 'echo "Docker GUI Tools:"' C-m
send-keys 'echo "• lazydocker  - Terminal UI"' C-m
send-keys 'echo "• Portainer   - http://localhost:9000"' C-m
send-keys 'echo "• docker ps   - Command line"' C-m

# Window 6: Logs and monitoring  
new-window -t 'saas-dev' -n 'logs' -c '/home/rclanan/dev/projects/work/saas-platform'
send-keys 'echo "Logs and Monitoring"' C-m

# Select the main window
select-window -t 'main'

# === COPY MODE SETTINGS (useful for remote sessions) ===

# Use vim keybindings in copy mode
setw -g mode-keys vi

# Copy mode bindings
bind-key -T copy-mode-vi v send-keys -X begin-selection
bind-key -T copy-mode-vi y send-keys -X copy-selection
bind-key -T copy-mode-vi r send-keys -X rectangle-toggle

# === PLUGIN SUPPORT ===
# Note: Install TPM (Tmux Plugin Manager) if you want plugin support
# git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

# List of plugins (uncomment if TPM is installed)
# set -g @plugin 'tmux-plugins/tpm'
# set -g @plugin 'tmux-plugins/tmux-sensible'
# set -g @plugin 'tmux-plugins/tmux-resurrect'
# set -g @plugin 'tmux-plugins/tmux-continuum'

# Initialize TMUX plugin manager (uncomment if TPM is installed)
# run '~/.tmux/plugins/tpm/tpm'